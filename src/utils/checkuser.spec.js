
describe('Check User', () => {
  describe('Check User', () => {
    it('redirect to not found route when user is null', () => {
        expect(true).toEqual(true);
    });

    it('redirect to search route when user is valis', () => {
      expect(true).toEqual(true);
    });
  });
});
